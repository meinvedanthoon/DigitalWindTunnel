name: Keep Streamlit App Awake
on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:
jobs:
  ping-app:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: |
          curl -sL "https://digitalwindtunnel-ksezhcqyb8xukhopjugclm.streamlit.app/" > /dev/null || true
          echo "App pinged successfully!"
